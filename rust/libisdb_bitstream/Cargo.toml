[package]
name = "libisdb_bitstream"
version = "0.1.0"
edition = "2021"
license = "GPL-2.0-or-later"
description = "Rust port of LibISDB BitstreamReader.cpp: bit-level reading of MPEG bitstreams"

[lib]
name = "libisdb_bitstream"
path = "src/lib.rs"

[dependencies]
