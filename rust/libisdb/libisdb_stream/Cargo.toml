[package]
name = "libisdb_stream"
version = "0.1.0"
edition = "2021"
license = "GPL-2.0-or-later"
description = "Rust port of LibISDB Base/Stream.hpp: stream abstraction + in-memory stream"

[lib]
name = "libisdb_stream"
path = "src/lib.rs"

[dependencies]
