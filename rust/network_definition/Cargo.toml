[package]
name = "tvtest_network_definition"
version = "0.1.0"
edition = "2021"
license = "GPL-2.0-or-later"
description = "Rust port of TVTest's CNetworkDefinition (network type / remote control key ID resolution)"

[lib]
name = "tvtest_network_definition"
path = "src/lib.rs"

[dependencies]
tvtest_string_utility = { path = "../string_utility" }
