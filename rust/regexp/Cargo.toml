[package]
name = "tvtest_regexp"
version = "0.1.0"
edition = "2021"
license = "GPL-2.0-or-later"
description = "Rust port of TVTest's RegExp: CRegExp ECMAScript regex wrapper with IgnoreWidth support"

[lib]
name = "tvtest_regexp"
path = "src/lib.rs"

[dependencies]
regex = "1"
bitflags = "2"
