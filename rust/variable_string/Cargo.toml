[package]
name = "tvtest_variable_string"
version = "0.1.0"
edition = "2021"
license = "GPL-2.0-or-later"
description = "Rust port of TVTest's VariableString core (%keyword% format / separators / event title-mark / filename normalize)"

[lib]
name = "tvtest_variable_string"
path = "src/lib.rs"

[dependencies]
tvtest_string_utility = { path = "../string_utility" }
tvtest_util = { path = "../util" }
