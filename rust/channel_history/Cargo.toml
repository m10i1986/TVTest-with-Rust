[package]
name = "tvtest_channel_history"
version = "0.1.0"
edition = "2021"
license = "GPL-2.0-or-later"
description = "Rust port of TVTest's CChannelHistory / CRecentChannelList core (history navigation, LRU dedup)"

[lib]
name = "tvtest_channel_history"
path = "src/lib.rs"
