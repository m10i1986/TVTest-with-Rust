[package]
name = "libisdb_bitrate"
version = "0.1.0"
edition = "2021"
license = "GPL-2.0-or-later"
description = "Rust port of LibISDB BitRateCalculator.cpp: bit rate measurement with injectable clock"

[lib]
name = "libisdb_bitrate"
path = "src/lib.rs"

[dependencies]
