# TVTest の C++ コードを Rust へ移植したクレート群をまとめるワークスペース。
# 各モジュールは原実装(src/*.cpp)とプラットフォーム非依存な範囲で挙動を一致させ、
# Rust の単体テストで等価性を検証する。
[workspace]
resolver = "2"
members = [
    "string_utility",
    "path_util",
    "util",
    "winutil",
]
